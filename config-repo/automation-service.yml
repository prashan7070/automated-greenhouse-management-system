server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agms_automation_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.agms: DEBUG