server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///D:/Microservice project/automated-greenhouse-management-system/config-repo

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/